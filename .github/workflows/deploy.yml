name: Deploy to Oracle Cloud

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

env:
  DOCKER_IMAGE: jobtracker
  DOCKER_TAG: ${{ github.sha }}

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run linting
        run: npm run lint || echo "No lint script found"

      - name: Run tests
        run: npm test || echo "No tests configured yet"

  build:
    name: Build Docker Image
    runs-on: ubuntu-latest
    needs: test

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Oracle Cloud Container Registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ secrets.OCI_REGISTRY }}
          username: ${{ secrets.OCI_USERNAME }}
          password: ${{ secrets.OCI_AUTH_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: ${{ github.event_name != 'pull_request' }}
          tags: |
            ${{ secrets.OCI_REGISTRY }}/${{ secrets.OCI_NAMESPACE }}/${{ env.DOCKER_IMAGE }}:${{ env.DOCKER_TAG }}
            ${{ secrets.OCI_REGISTRY }}/${{ secrets.OCI_NAMESPACE }}/${{ env.DOCKER_IMAGE }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    name: Deploy to OCI
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.8.0
        with:
          ssh-private-key: ${{ secrets.OCI_SSH_PRIVATE_KEY }}

      - name: Add OCI host to known hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H ${{ secrets.OCI_HOST }} >> ~/.ssh/known_hosts

      - name: Deploy to OCI Compute Instance
        env:
          OCI_HOST: ${{ secrets.OCI_HOST }}
          OCI_USER: ${{ secrets.OCI_USER }}
          DOCKER_IMAGE_TAG: ${{ secrets.OCI_REGISTRY }}/${{ secrets.OCI_NAMESPACE }}/${{ env.DOCKER_IMAGE }}:${{ env.DOCKER_TAG }}
        run: |
          ssh ${{ env.OCI_USER }}@${{ env.OCI_HOST }} << 'ENDSSH'
            set -e

            echo "=== Deploying Job Tracker Application ==="

            # Navigate to application directory
            cd /opt/jobtracker

            # Pull latest changes (if using git deployment)
            if [ -d .git ]; then
              git pull origin main
            fi

            # Pull latest Docker image
            echo "Pulling latest Docker image..."
            docker-compose pull app

            # Run database migrations
            echo "Running database migrations..."
            docker-compose run --rm app npx prisma migrate deploy

            # Restart application with zero-downtime
            echo "Restarting application..."
            docker-compose up -d --no-deps --build app

            # Check health
            echo "Waiting for application to be healthy..."
            sleep 10
            curl -f http://localhost:3000/health || exit 1

            # Cleanup old images
            echo "Cleaning up old Docker images..."
            docker image prune -f

            echo "=== Deployment completed successfully ==="
          ENDSSH

      - name: Verify Deployment
        run: |
          sleep 5
          curl -f https://${{ secrets.PRODUCTION_URL }}/health || exit 1

      - name: Notify on Success
        if: success()
        run: |
          echo "✅ Deployment successful!"
          echo "Application URL: https://${{ secrets.PRODUCTION_URL }}"

      - name: Notify on Failure
        if: failure()
        run: |
          echo "❌ Deployment failed!"
          echo "Check the logs above for details."

  rollback:
    name: Rollback on Failure
    runs-on: ubuntu-latest
    needs: deploy
    if: failure()

    steps:
      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.8.0
        with:
          ssh-private-key: ${{ secrets.OCI_SSH_PRIVATE_KEY }}

      - name: Rollback to Previous Version
        run: |
          ssh ${{ secrets.OCI_USER }}@${{ secrets.OCI_HOST }} << 'ENDSSH'
            cd /opt/jobtracker
            echo "Rolling back to previous version..."
            docker-compose down
            docker-compose up -d
            echo "Rollback completed"
          ENDSSH
